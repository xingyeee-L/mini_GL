# 阶段 9：桌面化与真实数据准入

## 目标

让 mini_GL 从开发原型走向可日常使用的本地产品，同时保证真实资料只在用户明确选择和授权后进入只读链路。

## 已完成的第一纵向切片

- [x] 增加 `desktop` 命令，启动回环服务后自动打开系统浏览器。
- [x] 增加 Windows 双击入口 `start-mini-gl.cmd`。
- [x] 禁止同一端口启动多个实例，避免浏览器命中残留旧后端。
- [x] 增加 Python、SQLite、数据库完整性、Ollama、Qwen 和索引规模诊断。
- [x] 增加跨全部已授权数据源的关键词和 BGE 混合搜索。
- [x] 增加跨全部已授权数据源的本地 RAG，并保留每条引用的来源身份。
- [x] 跨数据源搜索逐个来源执行只读 Agent 授权和审计。
- [x] 增加可视 SQLite 备份和恢复到新路径入口。
- [x] 真实目录注册要求网页端显式勾选授权确认。
- [x] 增加 Windows 原生文件夹选择器；选择动作只返回路径，不注册或读取正文。
- [x] 模型中心显示 Ollama、固定 Qwen、BGE 的真实就绪状态和恢复步骤。
- [x] 真实目录继续沿用扩展名、大小、深度、符号链接和重解析点防护。
- [x] 保持真实 QQ/微信客户端数据库禁区。

## 尚未完成

- [ ] 可直接安装/启动模型的首次启动向导；当前只提供诊断和恢复步骤。
- [ ] 会话历史的本地持久化和单独删除。
- [ ] 备份计划、备份列表和从恢复副本重启的完整流程。
- [ ] 卸载前清理预览、精确确认和完整残留清单。
- [ ] PDF、DOCX 等文档解析器及其专项安全测试。
- [ ] 桌面安装包、签名、升级和回滚机制。
- [ ] 第一个真实聊天导出格式的最小脱敏样本审查。

## 真实资料准入步骤

1. 用户在“控制台 → 数据与索引”填写自己明确选择的目录。
2. 用户确认拥有目录中资料的使用权限。
3. 系统只注册规范化根目录，不在注册动作中扫描正文。
4. 用户主动点击同步后，系统按原有只读安全扫描策略处理 `.txt` 与 `.md`。
5. 同步完整成功后才提交文件状态；失败整轮回滚。
6. 用户主动建立关键词和 BGE 索引后，资料才进入搜索与本地回答。

## 完成标准

- 双击入口能可靠启动并展示可操作的恢复提示。
- 用户不使用命令行即可诊断、备份、恢复和准入资料。
- 跨数据源结果保持来源和权限隔离。
- 卸载或撤销前能列出所有应用自有派生数据，且绝不删除原始资料。
- 第一个真实导出器经过专项审查后才从“预留”变为“支持”。
